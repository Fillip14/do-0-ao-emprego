info:
  name: patch-task
  type: http
  seq: 7

http:
  method: PATCH
  url: http://localhost:3000/tasks/1
  body:
    type: json
    data: '{"title": "Novo titulo"}'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
