info:
  name: post-task-invalid
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:3000/tasks
  body:
    type: json
    data: '{"title": "Titulo", "status": "todo"}'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
