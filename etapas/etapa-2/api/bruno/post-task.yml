info:
  name: post-task
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3000/tasks
  body:
    type: json
    data: '{"title": "Titulo", "status": "todo"}'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
