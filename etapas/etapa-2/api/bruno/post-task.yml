info:
  name: post-task
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3000/tas
  body:
    type: json
    data: '{titulo: "Titulo"}'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
