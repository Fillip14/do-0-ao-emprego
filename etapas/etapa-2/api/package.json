{
  "name": "api",
  "version": "1.0.0",
  "description": "Estudo node/express",
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "test": "PGHOST=/var/run/postgresql PGDATABASE=tasks_test vitest",
    "start": "PGHOST=/var/run/postgresql PGDATABASE=tasks node server.js",
    "dev": "PGHOST=/var/run/postgresql PGDATABASE=tasks node --watch server.js"
  },
  "dependencies": {
    "express": "^5.2.1",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "supertest": "^7.2.2",
    "vitest": "^4.1.10"
  }
}
